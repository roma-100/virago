{% macro category_item(image,title, link) %}

  <div class="product-category-card" onclick={{link}} >
    <div class="product-category-card__image">
      <img src={{image}} alt={{title}}>
    </div>
    <div class="product-category-card__text">
      {{title}}
    </div>
  </div>

{% endmacro %}